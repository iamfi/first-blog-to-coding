# "/" + 仓库的名字. 资源文件的路径组成部分
baseurl: '/first-blog-to-coding'

# 网页的标题
title: "Girl Meet Tech"
# 网站的主标题
name: "1 hour challenges to Coding"
description: "To mark every step of our success :) "

# 其它配置, 可忽略, 无需修改
url: site url
icon: your site icon path # /assets/img/icon.png [best 300px X 300px]
disqus: disqus_shortname # get it on https://disqus.com/admin/

defaults:
  - scope:
      path: "_posts"
      type: "posts"
    values:
      layout: "default"

permalink: /:title/

markdown: kramdown

kramdown:
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    block:
      line_numbers: true
exclude: [ changelog.md, LICENSE.txt, README.md, Gemfile, Gemfile.lock ]
